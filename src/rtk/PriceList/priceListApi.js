import axios from "axios";

let a = "194.87.239.231:55555";

export const priceListApi = {
  //process.env.REACT_APP_IP
  fetchData: (id) => {
    //return axios.get(`http://${a}/api/Document/${id}`);
  },
};
